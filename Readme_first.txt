1.extract files in one folder and open cmd with that folder directory.
2.run command as a "python main.py"

If dash modules and other not install please install By running cmd as admin and run the main.py file


pip install dash

pip install dash-bootstrap-components

pip install DateTime

pip install scipy

pip install maths

pip install plotly

pip install pandas

pip install numpy

